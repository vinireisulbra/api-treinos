info:
  name: 01 - Listar
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/treinos"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
