info:
  name: 04 - Atualizar
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/treinos/3"
  body:
    type: json
    data: |-
      {
        "nome": "Treino C - Pernas (atualizado)",
        "grupoMuscular": "Pernas",
        "series": 5,
        "repeticoes": 12,
        "carga": 45.0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
